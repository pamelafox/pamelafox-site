[tool.black]
line-length = 100
target-version = ['py39']

[tool.pytest.ini_options]
addopts = "-ra --cov"
testpaths = [
    "tests"
]

[tool.coverage.paths]
source = ["app"]

[tool.coverage.report]
show_missing = true
fail_under = 100
